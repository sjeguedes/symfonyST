{# https://github.com/symfony/symfony/blob/master/src/Symfony/Bridge/Twig/Resources/views/Form/form_div_layout.html.twig #}

{# Errors #}
{%- block form_errors -%}
    {%- if errors|length > 0 -%}
        {# CAUTION: this defines a global way to display all forms errors! #}
        <ul class="uk-list uk-flex">
            {%- for error in errors -%}
                <li class="uk-label uk-flex uk-flex-middle uk-form-danger uk-box-shadow-large uk-text-bold uk-text">
                    <span uk-icon="icon: arrow-down"></span>
                    <span class="uk-display-inline-block">
                         {{ error.message|nl2br }}
                    </span>
                </li>
            {%- endfor -%}
        </ul>
    {%- endif -%}
{%- endblock form_errors -%}

{# Inputs #}
{%- block form_widget_simple -%}
    {%- set type = type|default('text') -%}
    {%- if type == 'range' or type == 'color' -%}
        {# Attribute "required" is not supported #}
        {%- set required = false -%}
    {%- endif -%}
        <input type="{{ type }}" {{ block('widget_attributes') }} {% if value is not empty %}value="{{ value }}"{% endif %}/>
{%- endblock form_widget_simple -%}

{# Checkboxes #}
{%- block checkbox_widget -%}
    <input type="checkbox" {{ block('widget_attributes') }} {% if value is not empty %}value="{{ value }}"{% endif %}{% if checked %} checked="checked"{% endif %} />
{%- endblock checkbox_widget -%}

{# Images Collection field #}
{#{%- block _create_trick_images_widget -%}
    #}{# the inner ImageToCropType #}{#
    Ceci est un test.
    {{- block('_create_trick_images_entry_image_to_crop_widget') -}}
{%- endblock -%}#}

{#{% block _create_trick_images_entry_image_to_crop_widget %}
    {{- block('_create_trick_images_entry_image_to_crop_entry_image_widget') -}}
{% endblock %}#}

{#{% block _create_trick_images_entry_image_to_crop_entry_image_widget %}
    #}{# the inner image field #}{#
    {{- block('form_widget_simple') -}}
    {{ full_name }}
{% endblock %}#}

{#{% block _create_trick_images_entry_image_to_crop_entry_description_widget %}
    #}{# the inner image field #}{#
    <textarea {{ block('widget_attributes') }}>{{ value }}</textarea>
    {{ full_name }}
{% endblock %}#}

{#{% block _task_manager_task_lists_entry_tasks_entry_name_* %}
    #}{# the field of TaskType #}{#
{% endblock %}#}
